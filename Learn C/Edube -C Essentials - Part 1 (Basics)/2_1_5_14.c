#include <stdio.h>

int main()
{
	int daysInWeek;
	float pi;
	printf("How many days are in a week: ");
	scanf("%d",&daysInWeek);
	printf("What is the value of Pi to 2 points: ");
	scanf("%f",&pi);
	printf("There are %d days in a week\n",daysInWeek);
	printf("Pi value is %f\n",pi);
	return 0;
}